import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const AdCreateScreen = () => {
  return (
    <View>
      <Text>AdCreateScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
